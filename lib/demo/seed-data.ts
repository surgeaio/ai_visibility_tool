/**
 * Canonical demo seed for repositories and UI when `isAuthBypassMode()` / demo paths apply.
 * Re-exports `lib/demo-data.ts` so callers can import from `@/lib/demo/seed-data` (Sprint A.4).
 */
export * from "../demo-data";
